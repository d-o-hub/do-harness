# Heuristics
- **assertions that pass without the guidance payload lower measured Skill Lift, and the raise-only lift floor blocks the skill until guidance-dependent coverage is restored; add a documentation assertion tied to the new guidance when extending a blessed skill**: skill changes that add script- or artifact-level assertions to an evaluated corpus (from trace 39)
