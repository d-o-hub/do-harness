{
  "skill_name": "event-modeler",
  "evals": [
    {
      "id": 1,
      "prompt": "Model a state-change slice for creating a user with an email.",
      "expected_output": "A Command struct, an Event enum with serde derives and #[serde(deny_unknown_fields)], and a typed thiserror error enum, split across commands.rs, events.rs, and errors.rs.",
      "files": [],
      "assertions": [
        "Command struct has typed fields",
        "Event enum derives Serialize/Deserialize with #[serde(deny_unknown_fields)]",
        "Errors use thiserror, not anyhow"
      ]
    },
    {
      "id": 2,
      "prompt": "Design the read-side projection for a user summary view.",
      "expected_output": "A UserSummaryView struct with an apply(&mut self, event) method that updates total_users and last_registered_user from UserCreated events.",
      "files": [],
      "assertions": [
        "Projection struct fields match the requested view",
        "apply method takes &mut self and the event type",
        "File stays under 500 LOC"
      ]
    }
  ]
}