---
name: release-publisher
description: >
  Publish tagged releases: build static binaries, write checksums, and push platform packages to the registry from a release workflow.
license: MIT
metadata:
  version: "0.1.0"
  short-description: Publish a tagged release
---

# release-publisher

Body prose for release-publisher. This text must never be sent to a selector or appear
in suggestion output; only the frontmatter metadata above is.
