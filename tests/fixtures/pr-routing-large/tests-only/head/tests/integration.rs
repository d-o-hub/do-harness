use crate::CASES;

#[test]
fn case_01_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&1), "case 1 must stay registered");
}

#[test]
fn case_02_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&2), "case 2 must stay registered");
}

#[test]
fn case_03_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&3), "case 3 must stay registered");
}

#[test]
fn case_04_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&4), "case 4 must stay registered");
}

#[test]
fn case_05_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&5), "case 5 must stay registered");
}

#[test]
fn case_06_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&6), "case 6 must stay registered");
}

#[test]
fn case_07_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&7), "case 7 must stay registered");
}

#[test]
fn case_08_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&8), "case 8 must stay registered");
}

#[test]
fn case_09_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&9), "case 9 must stay registered");
}

#[test]
fn case_10_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&10), "case 10 must stay registered");
}

#[test]
fn case_11_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&11), "case 11 must stay registered");
}

#[test]
fn case_12_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&12), "case 12 must stay registered");
}

#[test]
fn case_13_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&13), "case 13 must stay registered");
}

#[test]
fn case_14_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&14), "case 14 must stay registered");
}

#[test]
fn case_15_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&15), "case 15 must stay registered");
}

#[test]
fn case_16_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&16), "case 16 must stay registered");
}

#[test]
fn case_17_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&17), "case 17 must stay registered");
}

#[test]
fn case_18_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&18), "case 18 must stay registered");
}

#[test]
fn case_19_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&19), "case 19 must stay registered");
}

#[test]
fn case_20_is_registered() {
    let ids: Vec<u32> = CASES.iter().map(|case| case.id).collect();
    assert!(ids.contains(&20), "case 20 must stay registered");
}

