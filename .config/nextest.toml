[profile.default]
retries = 0
# No `no-tests` key here: nextest reports it as an unknown configuration key, and
# every `nextest run` in the harness and the scaffold passes --no-tests=pass.

[profile.ci]
retries = 1
slow-timeout = { period = "60s", terminate-after = 3 }
